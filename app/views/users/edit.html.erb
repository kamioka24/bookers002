<main>
	<h1>User info</h1>

	<%= form_for(@user) do |f| %>
		<p>Name
		<%= f.text_field :name %></p>

		<p>Image</p>
		<%= f.attachment_field :profile_image %>

		<p>Introduction
		<%= f.text_area :introduction %></p>

		<p><%= f.submit 'Update User' %></p>

	<% end %>
</main>